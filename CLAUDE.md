# CLAUDE.md — JARVIS Mobile

Working notes for AI/coding agents (and humans) in this repository. Keep it
current: goals, constraints, build commands, conventions, and **phase state**.

## Product goal

An **offline-first** Italian personal assistant for Android, tuned for an
HONOR 200 (MagicOS, Snapdragon 7 Gen 3) with AirPods over Bluetooth. Core loop:
button/Tile → route mic to AirPods → capture → local VAD → offline STT → optional
memory retrieval → router → local LLM (or PC/Home Assistant when authorized) →
offline TTS → follow-up window. **Everything essential must work in airplane mode.**

## Non-negotiable constraints

See `docs/PRIVACY.md` and `docs/SECURITY.md` for the full list. Highlights:

- Offline-first; privacy by default; no mandatory account/cloud.
- No hidden recording; no always-on background mic in the default config.
- Any background mic use is a foreground service + persistent notification.
- No Accessibility abuse, no `MANAGE_EXTERNAL_STORAGE`, no arbitrary shell from the model.
- No secrets/personal content in logs. Secrets only in Android Keystore.
- The Obsidian vault is the human-readable source of truth; vector DBs are rebuildable caches.
- Every engine is swappable behind an interface.

## Repository layout

```
core/     Standalone pure-Kotlin domain (NO Android). Compiles & unit-tests on any JVM.
          state machine · router · tool protocol · policies · retrieval ranking · redaction
app/      Android application (Compose, Hilt, Room, audio, services). Consumes :core
          as a composite (included) build.
server/   (Phase 8) optional FastAPI companion for a home PC. Not started yet.
docs/     Architecture, plans, security, privacy, models, tests, device guide, decisions.
```

## Build & test commands

**Domain core (works anywhere, incl. CI without the Android SDK):**
```bash
cd core && ./gradlew test          # pure-Kotlin core suite
```

**Android app (requires the Android SDK + AGP; see "Environment" below):**
```bash
./gradlew :app:assembleDebug       # build debug APK
./gradlew :app:testDebugUnitTest   # JVM unit tests
./gradlew :app:lint                # Android lint
./gradlew :app:connectedDebugAndroidTest   # instrumented/Compose tests (device/emulator)
```
Debug APK output: `app/build/outputs/apk/debug/app-debug.apk`.

## Environment note (important for agents)

The container used to scaffold this project has **JDK 21, Gradle 8.14.3, CMake,
Python** but **no Android SDK/NDK**, and its egress policy **blocks
`dl.google.com`** — the host that serves the Android SDK, the Android Gradle
Plugin and all AndroidX/Compose/Material3 artifacts. Consequences:

- The `core/` module **is** compiled and tested here (Maven Central is reachable).
- The `app/` module **cannot** be compiled here. It is written to build in Android
  Studio / any environment with SDK access. Do not claim it compiles until it has
  been built somewhere with the SDK.
- Model files (GGUF, sherpa-onnx) and Hugging Face are also blocked/here-absent;
  models are imported by the user on-device (see `docs/MODELS.md`).

## Conventions

- Kotlin, Compose, Material 3, Gradle Kotlin DSL, version catalog (`gradle/libs.versions.toml`).
- Unidirectional data flow: `ViewModel` → `StateFlow` → Compose. Domain logic lives in `:core`.
- DI with Hilt. Room for structured data, DataStore for prefs, Keystore for secrets.
- Interfaces first (`AudioRouteManager`, `SpeechToTextEngine`, `LlmEngine`, …); Android
  impls bound in `di/`. Fakes for tests.
- Italian UI strings in `res/values/`. Technical logs only, always redacted (`LogRedactor`).
- Never commit models, tokens, audio, or personal notes.

## Phase state

| Phase | Scope | Status |
|------|-------|--------|
| 0 | Foundations: project, DI, Compose, docs, CI | **Done (build-verified)** — core 71 tests green; Android app compiles + debug APK produced by CI (run #2, `f8e94dd`) |
| 1 | Audio loop: button/Tile/service/routing → fixed TTS in AirPods | **Verified on HONOR 200** — real AudioRecord capture confirmed on-device (build `7342e6a`); press → record → fixed TTS reply. No STT/LLM by design. Root causes fixed on the way: stable debug signing (updates now install in place) and a MagicOS foreground-service/audio-focus conflict (orb now uses the same minimal capture path as the working diagnostics test). |
| 2 | STT (offline) | **Verified on HONOR 200** (build `8ee7275`) — offline Android on-device recognizer transcribes Italian; Home shows partial + transcript, Diagnostics has an STT test. sherpa-onnx (bundled, model-import) can replace it behind `SpeechToTextEngine`. VAD via recognizer end-of-speech. |
| 3 | Local LLM + model manager | **Verified on HONOR 200** (build `045bb77`) — engine is **LiteRT-LM** (`.litertlm`, `com.google.ai.edge.litertlm:litertlm-android`) behind `LlmEngine`; a `.litertlm` Gemma imported via the Models screen loads and generates on-device. Migrated off the MediaPipe LLM `.task` path (maintenance-mode API, corrupt-download "Unable to open zip archive"). The migration also lifted the toolchain: Kotlin 2.2.21, KSP 2.2.21-2.0.4, Hilt 2.57.2. **Multi-turn memory:** each fast/advanced engine keeps its own persistent `Conversation` (KV cache); the coordinator detects model switches/reloads and reseeds the destination from the shared bounded transcript so the two slots behave as one assistant. `maxNumTokens=4096`; "Nuova conversazione" clears both. Latest context-sharing changes are pending CI/device verification. |
| 4 | Full TTS + audio focus + follow-up | **Implemented, expanded (pending current CI/device check)** — installed offline voice selection, bounded rate/pitch, audio focus, hands-free follow-up, visible mic/orb barge-in, native Android Assistant-role activation and opt-in spoken worker replies. Background speech is off by default and uses a visible media-playback FGS. No custom always-on hotword. |
| 4b | External local neural voices | **Implemented (pending CI/device check)** — two optional ONNX voices behind the `NeuralTtsEngine` interface, **Kokoro v1.0** and **Piper (VITS)**, selectable in Impostazioni › «Voce JARVIS». Piper takes `<voice>.onnx` + `<voice>.onnx.json` (the config carries the model's own `phoneme_id_map`, sample rate and scales, so its token ids need no reconstruction); Kokoro takes `.onnx` + `voices-v1.0.bin`, which is never offered to Piper. File paths are stored per engine. Status shows «Pronto» only after a real runtime+model init. sherpa-onnx was evaluated and rejected: it is published on neither Maven Central nor a reachable JitPack, and ONNX Runtime already runs both graphs directly. Earlier notes: optional Kokoro v1.0 ONNX voice behind a new `NeuralTtsEngine` interface, selected in Impostazioni › «Voce JARVIS». The user imports `.onnx` + `voices-v1.0.bin` (and optionally `tokens.txt`/`config.json`); files are copied into app-private storage, the session loads lazily and stays warm between replies, and long answers are synthesised sentence by sentence into a streaming `AudioTrack`. Phonemes come from a rule-based Italian G2P in `:core` (no espeak-ng bundled). `HybridTtsEngine` picks neural or Android per utterance, so `SessionCoordinator`, STT and transcription are untouched. Nothing is bundled in the APK and inference never touches the network. |
| 4c | Foreground wake word | **Implemented (pending CI/device check)** — an **opt-in, app-open-only** wake word (default: the assistant's name) that starts a listening session hands-free. It runs **only while the home screen is resumed** (ON_RESUME→start, ON_PAUSE→stop), never in the background, and needs no permission beyond the mic. Behind a swappable `WakeWordEngine`: the current impl loops the on-device recognizer (its own instance, so it can never abort a real session) and matches via the pure `:core` `WakeWord`; a dedicated ONNX keyword-spotter can drop in later (sherpa-onnx isn't fetchable in this build env, verified). Settings toggle + word; the orb shows «In ascolto della parola». This is distinct from the rejected *always-on* hotword. |
| 5 | Memory V2 | **Implemented (pending current CI/device check)** — bounded app-private short recap plus stable editable records in `JARVIS/Memoria.md`; Temporary/Permanent/Sensitive types, visible sensitive marker, credential rejection, CRUD screen, bounded retrieval and Obsidian edit resync. Chat/voice writes use `CONFIRMING_WRITE`; no delicate fact is silently promoted to the vault. See ADR 0007. |
| 6 | Controlled Android tools | **Implemented, expanded (pending current CI/device check)** — original clock/battery/timer/alarm/torch/agenda/calculate tools plus app/settings allowlists, explicit external-calendar/call/SMS drafts, navigation, media search/control, confirmed active-notification reads and selected-vault search. Arguments always come from user text. No address book, direct call/send/save, broad package/storage/contact/calendar permission or arbitrary Intent. See ADR 0008. |
| 6b | Personal structured calendar | **Implemented (pending device check)** — `:core` `agenda` package (`AgendaEntry`, `ItalianDateTimeParser`, `Agenda`): a timed item is an appointment and an untimed item is a dated task, never "domani" hidden in prose. `AgendaRepository` stores both in `JARVIS/Agenda.md` (app-private fallback with later vault migration), supports alerts and confirmed completion, and the seven-day dashboard reads the real file. Generic calendar requests save locally; Google/Android Calendar is explicit draft export only. Clock arithmetic stays deterministic via `time_until`. See ADRs 0004 and 0010. |
| 6b+ | Unified tasks (Google-Tasks-style) | **In progress (pending CI/device check)** — the one planner entry now also carries task fields: an **optional** due date (undated tasks like Google Tasks), a `list`, a `starred` flag, `notes`, one level of `parentId` sub-tasks, `order` and `completedAt`; all round-trip through `Agenda.md` metadata backward-compatibly (`:core` tests). The «Attività» screen is rebuilt as a Google-Tasks UI: list tabs (+ new list, + ★ Speciali), quick add, ⭐/complete/due/move/sub-task/rename/notes/delete per task, and a collapsible «Completate» archive with completion dates. Local/offline only — no Google account/sync (offline-first). Reminders skip undated tasks. Next: chat/voice creation of lists & stars, a full date picker, and device verification. |
| 6c | Understanding V3 | **Implemented (pending CI/device check)** — knowledge/conversation messages, including multiple questions, now take one coherent model turn. Deterministic tools run first; only plausible unfamiliar operations invoke the one-line LLM classifier. Low-confidence output cannot execute a tool, and generated role/template continuations are removed. |
| 6d | Persistent response queue | **Implemented (pending CI/device check)** — typed requests are persisted in Room and run by a long-running WorkManager worker with visible progress, real native cancellation, retry and idempotent chat writes. The chat send control becomes Stop while active; a 90-second native watchdog prevents infinite inference. Model load, memory retrieval and generation survive Activity closure/process recreation. A private “response ready” notification opens the chat; preview is opt-in. |
| 6e | Reminder engine | **Verified on HONOR 200** — exact-alarm reminders arrive on time with the app closed. Agenda entries keep stable IDs plus zero/multiple alert rules in human-readable Markdown metadata. Dashboard choices: due time, morning-of, 1/2/3/7 days before, custom time, or none. WorkManager/exact-alarm path persists notifications across app exit/reboot; reconciliation handles edits/deletes and the morning hour is configurable. |
| 6f | Automazioni | **Implemented, expanded (pending CI/device check)** — recurring ("ogni giorno alle 8") and conditional ("quando la batteria…") rules, plus a new one-shot **deferred command**: "alle 11.45 accendi la torcia" becomes a `Trigger.Once` + `Action.Tool` rule instead of firing the torch now. The tool action passes the existing `CommandMatcher` at save *and* fire time and is confined to a headless subset of the command allowlist (`flashlight`, `media_control`); every firing posts a visible notification. Scheduled on the same exact-alarm path as reminders; fires once, then is spent. See ADR 0011. Dashboard agenda block rebuilt as date + weekly strip (active day ringed) + vertical timeline with «In arrivo»/«Completato» badges. |
| 6g | Live Translator | **Implemented (pending CI/device check)** — bidirectional **offline** voice translation between two of it/en/es/fr/ja, on a path that never touches the assistant's LLM. `:core` holds the tested pure logic (`TranslationLanguage`, `TranslationSegmenter`, `LanguageStabilizer`+`LivePairRouter`, `LiveTranslatorCommands`, `TranslationMode`/state/session — 15 tests). The Android layer is **ML Kit** on-device: `MlKitTranslationEngine` (translate, per-pair warm clients), `TranslationModelManager` (`RemoteModelManager` download/delete, Wi-Fi-only), `MlKitLanguageDetector` (language-id → supported set + confidence), `TranslationAudioController` (dedicated multilingual `TextToSpeech` for the target language, with a `speaking` flag for echo avoidance), and `LiveTranslatorManager` — the sequential **listen → segment → detect → translate → speak** loop reusing the existing offline STT. Modes AUTO/CONVERSAZIONE/PUSH_TO_TALK/SOLO_TESTO/VOCE_TESTO/SOLO_ASCOLTO; `LiveTranslatorScreen`, a dashboard tile, a Settings section (offline-language download), and voice control ("avvia traduzione live italiano giapponese" / "ferma" / "scambia le lingue") routed in `SessionCoordinator` before the LLM path. Only preferences are persisted, never a transcript. **Honest limit:** the platform recognizer transcribes one language at a time, so AUTO flips sides via the stabilizer once the other speaker has said enough — not simultaneous dual capture. ML Kit deps resolve on CI (Google Maven), not in the scaffolding container, so the app layer is CI-verified only; real-time feel and echo avoidance are device-pending. |
| 6h | Backup local-first | **Implemented (CI-verified compile, pending device check)** — nightly evening backup (default 23:30, configurable) that runs offline. `:core` holds the tested pure logic (`ManifestCodec` JSON round-trip, `Incremental` copy-only-changed diff, GFS `Retention` 7/4/6 — 8 tests). The Android layer: `BackupCrypto` (AES-256-GCM, key in the Android Keystore, length-prefixed IV), `BackupRepository` (incremental encrypted+compressed snapshot of JARVIS's own data — Room DB + WAL/SHM, DataStore prefs, vault notes; heavy re-downloadable dirs models/navigation/documents/knowledge recorded **manifest-only, never copied**; hash verify; retention prune; full + selective restore with a **pre-restore safety backup**; JARVIS-scoped vault write-back), `CloudBackupProvider` interface + `NoCloud`/`GoogleDrive` providers + `CloudSyncManager` (persisted offline upload queue, retries when deferred), `BackupWorker` + `BackupScheduler` (WorkManager periodic job with Wi-Fi/charging/battery constraints, backoff, manual "run now", a single discrete notification only on a failed backup; re-armed on app start). Backup prefs in `SettingsRepository`; "Backup e sincronizzazione" screen + ViewModel opened from Impostazioni. The cloud is always a later copy, never the source of truth. **Local destination folder:** by default backups live in app-private storage (invisible, lost on uninstall); the user can pick a real folder via the Storage Access Framework (`ExternalBackupStore`, persisted RW tree URI) — each snapshot is mirrored there (`JARVIS-Backups/<id>/backup.enc`+`manifest.json`, still encrypted), it survives uninstall, restore/verify/list read from it (importing back into internal on demand), and it self-prunes with the same GFS retention over its own contents so a first backup after a reinstall never wipes the surviving copies. **Real Google Drive sync (CI-verified, pending device check — currently being tested against the DEBUG build, see below):** `GoogleDriveBackupProvider` is no longer a stub. Auth is Android's Identity/`AuthorizationClient` API (`com.google.android.gms:play-services-auth`) against an **"Android"** OAuth client — matched by package name + signing certificate SHA-1 of the exact build under test (the debug build's `app/debug.keystore` and a later release build each need their own client registration; `docs/DEBUG_SIGNING.md` documents why the debug keystore's SHA-1 is stable enough to register), configured entirely in Google Cloud Console, no client id/secret in app code at all (`GoogleAuthManager`). The backup archive format itself (`ManifestCodec`, `BackupCrypto`/`BackupKeyManager` content-key envelope, the "JARVIS Backups" Drive folder) never references `applicationId`/build type, so a debug-created backup restores on a later release install via the Recovery Key — see ADR 0015. **Superseded design (ADR 0014, same-day):** an earlier GMS-free manual RFC 8252 browser-redirect flow against a "Desktop app" client — the user explicitly asked to switch to AuthorizationClient instead, having already set up the Android client themselves; see **ADR 0015**, which also documents that this makes Drive sync the one deliberate, narrowly-scoped Play-Services dependency in an otherwise GMS-free app. `requestOfflineAccess` (which would need a *second*, separate Web-application OAuth client) is deliberately not used — `GoogleAuthManager.currentAccessToken()` calls `authorize()` fresh each time, including from background Workers, resolving silently once consent is granted; Play Services owns all token refresh, nothing is stored by the app beyond a local "authorized" flag + cached email (`DriveCredentialStore`). Scope is **`drive.file`, not `drive.appdata`** — JARVIS creates and uses a "JARVIS Backups" folder that is **visible** in the user's own Drive (`GoogleDriveRestClient` finds-or-creates it, caches the id, self-heals if it goes stale), a real behavior change from the appdata-hidden design ADR 0014 first shipped. `GoogleDriveRestClient` talks to Drive v3 directly over OkHttp (upload/list/download/delete, post-upload MD5 check against Drive's own `md5Checksum`, one retry after a fresh `authorize()` on 401). `CloudSyncManager` gained cloud-side GFS retention pruning and the same list/import fallback `ExternalBackupStore` already had, so `listBackups()`/`restore()`/`verify()` transparently pull from the cloud when a backup exists nowhere locally (e.g. a fresh install). **Recovery key:** backup archives are now encrypted with a separate, software-held content key (`BackupKeyManager`) instead of the Keystore key directly — the Keystore key only wraps it for local storage, since Keystore keys cannot be exported by design and a new device restoring an old cloud backup needs *something* exportable. The content key can be revealed once as a human-copyable string (`RecoveryKeyCodec` in `:core`, Crockford Base32 + CRC-8, tested) and imported on another device; never shown by default, never written into the cloud archive it protects. |
| 6i | Intent routing strutturato | **Implemented (CI-verified, pending device check)** — un **Structured Path** fra il fast path e l'LLM, che porta all'agenda le operazioni che finora mancavano: delete/move/rename. `:core/intent` (puro, testato: `ResolvedIntent` con target/changes separati e `mayAct()`, `TextNormalizer`, tabelle alias dichiarative `IntentAliases`, `AgendaCommandParser`). Il parser isola **prima** la clausola di destinazione, così "porta l'appuntamento *di domani* *a lunedì*" non confonde il giorno che identifica l'impegno con quello di arrivo; gestisce shift relativi ("di due ore", "anticipalo di mezz'ora"), rename e forme contestuali ("spostalo alle 16", "quello del dentista cancellalo"). In `app/`: `AgendaIntentRouter` risolve le parole su impegni reali (uno → agisci, molti → chiedi quale, nessuno → dillo) e i tool `delete_agenda`/`move_agenda`/`rename_agenda` accettano **solo un id**, mai testo — quindi una frase vaga non può colpire il target sbagliato. Aggiunti `move_memory` (ricolloca un appunto in una categoria, rifiutando categorie inesistenti invece di farle scivolare in «Altro») e il vocabolario di conferma centralizzato: un "cancella" nudo durante una conferma **annulla** l'operazione, con un oggetto resta una cancellazione. Qualità misurata da `IntentMetricsTest` su corpus etichettato (57 frasi): precision 1.000, recall 1.000, 0 falsi positivi, 0 confusioni delete/move. Vedi ADR 0012. |
| 6j | Context + Automation engine (generico) | **Fasi 1-4 di 10, CI-verified — NON ancora collegato** — motore generico TRIGGER+CONDITIONS+ACTIONS costruito **accanto** a quello di 6f, che resta l'unico attivo. `:core` (puro, testato): `automation/rule` — `AutomationRule` (più trigger, albero di condizioni, più azioni, priorità, cooldown, oneShot, scadenza, schemaVersion), `Condition` AND/OR/NOT a valutazione **a tre valori** (un fatto ignoto non è mai vero e `NOT(ignoto)` resta ignoto, così nulla scatta a indovinare), `Registries` dichiarativi (18 trigger, 14 azioni, ognuno con `Capability` e `ActionRisk`), `RuleCodec` JSON versionato (una condizione illeggibile invalida l'intero albero invece di essere saltata, altrimenti la regola si allargherebbe), `LegacyRuleConverter` (converte solo ciò che ha equivalente esatto, il resto lo **segnala**), `RuleGate` (deduplica *prima* del cooldown, capability *prima* delle condizioni), `ConflictResolver` (priorità, pari merito stabili), `context/PlaceFusion` (noisy-OR, isteresi, dwell, debounce: il GPS che balla non genera arrivi a raffica). In `app/`: Room v4 (`automation_rules/places/executions/parking` + migrazione reale **prima** del fallback distruttivo, perché queste tabelle contengono dati che solo l'utente può produrre), `RuleRepository` (quarantena delle righe illeggibili), `CapabilityManager` (SUPPORTED/REQUIRES_PERMISSION/REQUIRES_USER_ACTION/UNSUPPORTED), `ActionHandlers` (4 reali: notifica, parlato, promemoria, run_tool via allowlist esistente), `AutomationExecutor` (executionId, timeout, dry-run, log anche dei **non** scatti), `ContextEngine` + sorgenti. Invariante blindata da test: un'azione dichiarata disponibile deve avere un handler — chiudeva la trappola di `ArrivedHome`, che esisteva nel modello mentre **nessun geofencing esiste nell'app**. Vedi ADR 0013. **Fasi 5-6 ora fatte e CI-verified — il motore è vivo (accanto a 6f):** fase 5 scheduler (`RuleSchedule` puro/testato → `RuleScheduler` su exact alarm `KIND_RULE` → `AlarmReceiver` costruisce `TriggerEvent`+`EvaluationContext` e chiama `AutomationExecutor.onTrigger()`, ri-armato a cold start e boot); fase 6 geofencing **senza Play Services** (`LocationManager.addProximityAlert`, `PlaceGeofenceSource` gated su `ACCESS_BACKGROUND_LOCATION`, `PlaceProximityReceiver` alimenta il `ContextEngine` e consegna l'evento; `PLACE_ENTER/EXIT` ora `implemented`, `TriggerMatching` puro/testato fa scattare una regola solo per il **suo** luogo). UI `RulesScreen`/`RulesViewModel` (Automazioni › «Regole avanzate») per salvare un luogo e creare regole orario/luogo → rende tutto provabile. `ACCESS_BACKGROUND_LOCATION` dichiarato. **Fase 7 (Activity Recognition) saltata deliberatamente** — nessuna API AOSP equivalente esiste, solo Google Play Services, e questa fase specifica del motore contesto/automazioni resta no-GMS; resta `UNSUPPORTED`, non `implemented=false` in attesa. (Nota fase 6h, più recente: il backup su Google Drive introduce poi una singola eccezione GMS mirata, `play-services-auth` per l'autorizzazione — vedi ADR 0015 — separata e indipendente da questo motore, che resta esso stesso GMS-free.) **Fase 8 (parcheggio) fatta, CI-verified, GMS-free**: `SAVE_PARKING_LOCATION` implementato — `SaveParkingActionHandler` legge l'ultima posizione nota (GPS/rete, nessun fix attivo) e la salva via `ParkingRepository`; card «Parcheggio» in Automazioni con salva/distanza/cancella. Il trigger automatico "fine modalità guida e non sono in un posto salvato" resta deliberatamente rimandato a dopo una futura modalità guida esplicita (fuori dalle 10 fasi). **Oltre alle 10 fasi originali, costruito nella stessa sessione (CI-verified):** `JarvisModeManager` (`core/mode/JarvisModes.kt` + `app/mode/JarvisModeManager.kt`) — la modalità esclusiva HOME/WORK/EVENING/SLEEP/DRIVING (`jarvis_mode`, azione `SET_JARVIS_MODE`) applica suoneria/vibrazione/silenzioso e Do-Not-Disturb (solo se l'accesso è concesso, mai finto) e spara `JARVIS_MODE_ENTER/EXIT`; DRIVING pre-arma la wake word esistente (mai un microfono in background: resta foreground-gated) e ripristina il valore precedente all'uscita. **Location Engine power modes**: `LocationPrecision` (LOW_POWER/BALANCED/HIGH_PRECISION) per modalità — dato che l'app non fa mai polling GPS continuo fuori da una sessione di navigazione attiva, l'unica leva reale è l'intervallo dei fix durante quella sessione, ora derivato dalla modalità corrente (`NavigationRepository.start()`); la parte Wi-Fi/Home-Assistant resta la Fase 7 (non iniziata). **Briefing mattutino adattivo**: esteso `ProactiveComposer.morningDigest` (compleanni letti dal testo agenda, pioggia solo se prevista, "Nessun impegno importante oggi" quando vuoto) e agganciato al vero primo sblocco (`ACTION_USER_PRESENT` → `AutomationEventService.onUnlock()` → `ProactiveManager.evaluateOnUnlock()`), col dedup giornaliero del governor esistente invece di un flag separato. **Backup Night Routine**: gate opzionale per luogo su `BackupWorker` (fail-open se il controllo stesso fallisce, mai un backup saltato per un bug). **Meteo (eccezione offline sanzionata)**: Open-Meteo opt-in, coordinate arrotondate, `Condition.RainToday/RainTomorrow` nel motore condizioni a tre valori, cutoff di 6h per lo staleness; "21:00 se domani piove" è già componibile nel Rule Builder esistente (RECURRING_TIME + SE pioggia domani + notifica), nessun codice nuovo necessario. Vedi `docs/PRIVACY.md` §"Sanctioned online exceptions". **Smart Departure esplicitamente rimandato** su richiesta dell'utente: l'utente darà indicazioni su come implementarlo dopo. **Mancano fasi 9-10**: Rule Builder QUANDO/SE/ALLORA con diagnostica/dry-run completa (oggi solo un builder minimo), AutomationDraft da linguaggio naturale, subentro al motore vecchio. |
| 6k | Modalità Guida (driving overlay) | **Implemented, CI-verified — pending device check** — overlay JARVIS "Sport Mode" sopra Google Maps *reale*, avviato da Intent (nessuna Maps/Navigation SDK a pagamento). `:core`: `driving/DrivingModels.kt` (`DrivingModeState`, `DrivingExpandedPanel` con mutua esclusione testata, `DrivingNavigationState` che non finge mai un ETA, mapping puro ed esaustivo `ConversationState → DrivingVoiceState`). In `app/`: `DrivingModeManager` (stato centrale + entry point unico, riusato anche dalla nuova azione `OPEN_DRIVING_HUD` del motore 6j), `DrivingModeService` (foreground `specialUse`, notifica persistente) che monta **tre finestre `WindowManager` separate** (barra alta/notifiche, nav compatta, dock Spotify) invece di un unico overlay "trasparente" — una sola finestra intercetterebbe comunque ogni tocco nei suoi confini, mentre più finestre piccole lasciano la mappa realmente **libera e toccabile** nel vuoto centrale. UI ricostruita nativamente in Compose (palette Sport rossa dell'HTML fornito, nessuna WebView, nessuna immagine bundlata) ospitata fuori da qualunque Activity tramite un `LifecycleOwner`/`ViewModelStoreOwner`/`SavedStateRegistryOwner` minimale. **Riuso totale, zero sistemi paralleli**: wake word esistente (`WakeWordController` esteso con un secondo flag indipendente `drivingModeActive`, mai un microfono nascosto — resta foreground-service + notifica), TTS/STT/LLM esistenti (`SessionCoordinator`), `JarvisNotificationListener` esteso con un tick di cambiamento live e una risposta rapida via `RemoteInput` reale (mai una risposta finta se Android non la conferma), `MediaSessionManager`/`MediaController` per Spotify (play/pause/next/prev + copertina reale, nessuna playlist/coda inventata quando la sessione non le espone), `JarvisModeManager` (entra/esce da DRIVING, **ripristina la modalità precedente alla chiusura**), `PlaceRepository` per risolvere "casa"/"lavoro"/luoghi salvati nei comandi di navigazione. Comandi vocali completi in `CommandMatcher` (attiva/chiudi modalità guida o moto, imposta navigazione per X, avvia percorso, mostra/riduci messaggi o Spotify, chi mi ha scritto, leggi il messaggio di X, rispondi a X: testo — quest'ultimo resta `EXTERNAL_COMMUNICATION` con conferma vocale, mai un invio silenzioso). Nessun ETA o istruzione turn-by-turn inventata: Google Maps non restituisce questi dati all'app che l'ha lanciata, quindi il pannello nav resta onestamente vuoto finché una fonte alternativa non sarà collegata. Al termine: salva la posizione del parcheggio se non è vicino a casa/lavoro/una posizione con "Francy" nel nome (`DrivingParkingGate`), cancellata automaticamente al prossimo arrivo a casa (`PlaceProximityReceiver`). Vedi `docs/PRIVACY.md` e `docs/SECURITY.md` per i permessi (`SYSTEM_ALERT_WINDOW` nuovo). **Driving Mode V2 (CI-verified, pending device check, dietro feature flag sviluppatore — questa modalità overlay resta il default utente):** nuova architettura che prepara il passaggio a una navigazione interamente interna a JARVIS, senza rompere l'overlay esistente. `core/driving`: `DrivingNavigationMode` (`EXTERNAL_MAPS_OVERLAY` default / `INTERNAL_JARVIS_NAVIGATION`), `DrivingUiState` (stato di presentazione agnostico dal provider, riusa `DrivingVoiceState`/`DrivingExpandedPanel`/`DrivingMediaState`/`DrivingNotification`/`ManeuverType` esistenti invece di duplicarli — test completi). **Scoperta in fase di analisi**: esisteva già uno stack di navigazione offline completo e non documentato qui (`app/.../navigation` + `core/.../navigation`) — MapLibre, router A* offline (`OfflineRoutingEngine`), map-matching, ricalcolo fuori-percorso, gestione PMTiles (`RegionManager`/`InstalledRegionStore`); la V2 lo riusa integralmente invece di ricostruirlo (vedi `docs/ARCHITECTURE.md` §"Modalità Guida / Driving Mode"). Nuova `DrivingModeActivity` (vera Activity Compose edge-to-edge, mai un overlay), raggiungibile solo dal flag sviluppatore in Diagnostica; `ui/navigation/JarvisMapView` estrae il MapLibre già usato da `NavigationScreen` in un componente condiviso; componenti Compose ispirati a `docs/design/jarvis_drive_reference.png` (`JarvisDriveTopBar`, `ManeuverCard`/`SpeedCard`/`EtaBar`, `VoiceDock`) più riuso diretto di `DrivingMediaPanel`/`NotifSheet` esistenti (mai una seconda implementazione di media/messaggi). Rilevamento chiamata in arrivo via `JarvisNotificationListener.hasActiveCall()` (categoria di sistema `CATEGORY_CALL`, nessun permesso telefonico nuovo). Stile mappa `jarvis-navigation.json` esteso con i layer `route-glow`/`route-outline`/`route-main` (alone rosso tenue, blu scuro intermedio, ciano centrale). Nessuna nuova dipendenza Gradle (MapLibre era già presente), nessun nuovo permesso oltre alla entry manifest di `DrivingModeActivity` (`exported=false`). Restano da fare: router dedicato tipo Valhalla (oggi `AStarRouter` copre il routing offline reale ma non è quello), camera di navigazione (`DrivingCameraController` con bearing/pitch dinamici), collegamento dei comandi vocali "portami a/vai a" al nuovo path interno. |
| 7 | Home Assistant | Not started |
| 8 | PC companion (`server/`) | Not started |
| 9 | Hardening / release | Not started |

**Definition of done for a phase:** the main chain compiles, unit tests pass,
lint is clean, docs/decisions updated. Never leave the main branch uncompilable.

## What is real vs. pending (honesty ledger)

- **Real & tested (JVM):** conversation state machine, hybrid router, tool JSON
  protocol + repair, tool registry/policies, calculate tool, log redactor,
  Markdown/frontmatter parser, retrieval ranker, Italian date/time parser +
  agenda model/formatting, Memory V2 codecs/summarizer and the neural-TTS layer
  (Italian G2P, phoneme vocabulary, NPZ voice-pack reader). → `cd core && ./gradlew test`.
- **Compiled + packaged by CI (GitHub Actions, Android SDK):** all of `app/` —
  audio route manager, real AudioRecord capture, offline TTS, listening foreground
  service, QS tile, Compose Home + Diagnostics, permissions, DI wiring. A debug APK
  is produced (`docs/ANDROID_BUILD_AUDIT.md`), signed with a committed stable debug
  key so updates install in place, and published to the `latest-debug` GitHub Release.
- **Verified on-device (HONOR 200, build `7342e6a`):** microphone capture on the
  main push-to-talk control (the Home orb), after fixing a MagicOS
  foreground-service/audio-focus conflict — the orb now uses the same minimal
  capture path as the Diagnostics "Test microfono".
- **Verified on-device (HONOR 200, build `045bb77`):** offline STT (Phase 2) plus
  the **LiteRT-LM** local LLM (Phase 3) — a user-imported `.litertlm` Gemma loads
  and generates a reply fully on-device, no network.
- **Real & tested (JVM):** the automation model + codec, including the new
  `Trigger.Once`/`Action.Tool` round-trip through `Automazioni.md`
  (`cd core && ./gradlew test`). The deferred-command parser and its allowlist
  re-check live in `app/` (they need the real `CommandMatcher`) and carry a
  `:app:testDebugUnitTest` suite that only CI can run.
- **Pending on-device verification (Phases 4 → 6e + automations/dashboard):**
  walked through function by function in `docs/DEVICE_TEST_PHASES_4_6E.md`. Do
  not promote any of these from "pending device check" until its section is ticked
  off on the phone.
- **Real & tested (JVM):** the structured intent layer — `ResolvedIntent`/`mayAct()`, the alias
  tables, the normalizer and `AgendaCommandParser`, plus a labelled-corpus metrics suite
  (`IntentMetricsTest`: precision/recall/confusion, false positives pinned at zero).
  → `cd core && ./gradlew test` (391 tests).
- **Real & tested (JVM):** il motore generico di automazione e il context engine —
  condizioni a tre valori, gate con deduplica/cooldown/capability, risoluzione conflitti,
  codec versionato, conversione legacy e fusione di posizione con isteresi/dwell/debounce.
  → `cd core && ./gradlew test` (488 tests).
- **Parzialmente attivo (fasi 5-6):** il motore 6j ora scatta su trigger a orario
  (exact alarm) e a luogo (geofence `addProximityAlert`, senza Play Services);
  `AutomationExecutor.onTrigger()` viene chiamato da `AlarmReceiver` e
  `PlaceProximityReceiver`. La UI «Regole avanzate» crea regole nel nuovo motore.
  Le automazioni **esistenti** dell'utente restano sul motore 6f (Markdown): il
  subentro (fase 10) è deliberatamente l'ultimo passo. Restano fasi 7-10
  (activity recognition, parcheggio, rule builder completo con condizioni,
  diagnostica/dry-run, AutomationDraft, hand-over).
- **Not implemented yet:** local document/Wikipedia knowledge, Room/FTS vault
  index, HA, PC server, custom wake word, benchmarks, release signing and the
  final instrumented/device acceptance suite.
